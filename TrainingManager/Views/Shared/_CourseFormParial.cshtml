@model BaseCourseVM


<div class="form-group">
    <label asp-for="Description"></label>
    <input asp-for="Description" class="input-control" />
    <span asp-validation-for="Description" class="text-danger"></span>
</div>
<div class="form-group">
    @* <select> *@
    @*     <option value="">Select A Category</option> *@
    @*     @foreach (var category in Enum.GetValues(typeof(CourseCategory)).Cast<CourseCategory>().ToList()) *@
    @*     { *@
    @*         <option value="@category">@category</option> *@
    @*     } *@
    @* </select> *@

    <select asp-for="Category" asp-items="@(Html.GetEnumSelectList<CourseCategory>())">
        <option value="">Select A Category</option>
    </select>
    <span asp-validation-for="Category" class="text-danger"></span>
</div>
<div class="form-group">
    <label asp-for="StartDate"></label>
    <input asp-for="StartDate" class="input-control" />
    <span asp-validation-for="StartDate" class="text-danger"></span>
</div>
<div class="form-group">
    <label asp-for="EndDate"></label>
    <input asp-for="EndDate" class="input-control" />
    <span asp-validation-for="EndDate" class="text-danger"></span>
</div>
<div class="form-group">
    <label asp-for="IsActive"></label>
    <input asp-for="IsActive" />
    <span asp-validation-for="IsActive" class="text-danger"></span>
</div>
<div class="form-group">
    <select asp-for="InstructorId" asp-items="@(ViewData["Instructors"] as IEnumerable<SelectListItem>)">
        <option value="">Select An Instructor</option>
    </select>
    <span asp-validation-for="InstructorId" class="text-danger"></span>
</div>